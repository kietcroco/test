import { colors as _colors } from '~/configs/styles';

const colors = {
    success: _colors.successColor,
	warning: _colors.warningColor,
	error: _colors.errorColor
};

const backgroundColors = {
    success: _colors.backgroundSuccess,
	warning: _colors.backgroundWarning,
	error: _colors.backgroundError
};

export {
    colors,
    backgroundColors
};